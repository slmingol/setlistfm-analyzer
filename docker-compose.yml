services:
  tour-app:
    build: .
    ports:
      - "3234:3000"
    volumes:
      # tours.db persists between restarts
      - tour-data:/app/data
      - ./top_artists.json:/app/data/top_artists.json
    env_file:
      - .env
    restart: unless-stopped

volumes:
  tour-data:
